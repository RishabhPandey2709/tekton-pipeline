apiVersion: v1
kind: Service
metadata:
  labels:
    app: tekton-demo
  name: tekton-demo
spec:
  type: NodePort
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: voting-ui